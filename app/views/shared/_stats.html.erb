<% @user ||= current_user %>
<div class="stats">
  <h2><a href="<%= following_user_path(@user) %>">
    <strong id="following" class="stat">
      Following <%= @user.following.count %>
    </strong>
  </a> | 
  <a href="<%= followers_user_path(@user) %>">
    <strong id="followers" class="stat">
    <%= @user.followers.count %> Followers
    </strong>
  </a></h2>
</div>
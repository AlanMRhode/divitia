<% if logged_in? %>
  <div class="center jumbotron"><h1>We Plan</h1></div>
    <div class="border col-md">
        <%= render 'shared/micropost_form' %>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
<div class="col-md">
  <div class="text-center">
  <%= image_tag("divitia.png") %><br>
  <%= link_to "Log In", login_path, class: "btn-lg btn btn-primary" %> 
	<%= link_to "Sign Up", signup_path, class: "btn-lg btn btn-primary" %>
</div>
</div>
<% end %>
